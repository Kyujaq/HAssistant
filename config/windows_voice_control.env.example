# Windows Voice Control Configuration
# Copy this file to .env or source it before running windows_voice_control.py

# Piper TTS Service URL
# If running in Docker, use the container name
TTS_URL=http://localhost:10200
# Or use Docker container name:
# TTS_URL=http://hassistant-piper-glados:10200

# Piper connection details (if using direct connection)
PIPER_HOST=hassistant-piper-glados
PIPER_PORT=10200

# USB Audio Device (ALSA)
# Use 'aplay -l' to find your device
# Format: hw:CARD,DEVICE (e.g., hw:1,0 for card 1, device 0)
USB_AUDIO_DEVICE=hw:1,0

# Use PulseAudio instead of ALSA (true/false)
USE_PULSEAUDIO=false

# PulseAudio sink name (if USE_PULSEAUDIO=true)
# Use 'pactl list sinks short' to find your sink name
PULSEAUDIO_SINK=alsa_output.usb-Generic_USB_Audio_Device-00

# Wyoming Protocol (alternative to HTTP)
# Set to true if your Piper service only supports Wyoming protocol
WYOMING_ENABLED=false

# Direct Piper TTS Configuration (recommended for Windows voice commands)
# Use direct Piper command for clearer voice instead of GLaDOS
# Set to true for maximum clarity with Windows Voice Assistant
USE_DIRECT_PIPER=true

# Piper executable path (if using direct Piper)
PIPER_EXECUTABLE=/usr/bin/piper

# Voice model for Windows commands (kathleen-high is clearer than GLaDOS)
# Options: en_US-kathleen-high, en_US-amy-medium, en_US-libritts-high
PIPER_VOICE_MODEL=en_US-kathleen-high

# Path to Piper voice models directory
PIPER_MODEL_PATH=/usr/share/piper-voices

# Speech length scale (1.0 = normal, 1.1 = 10% slower for better clarity)
# Higher values make speech slower and clearer for voice recognition
PIPER_LENGTH_SCALE=1.1

# Volume boost multiplier (1.0 = normal, 1.5 = 150% louder)
# Increase if Windows doesn't hear the commands reliably
# Requires sox or ffmpeg to be installed
PIPER_VOLUME_BOOST=1.0
